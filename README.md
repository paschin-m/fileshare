# fileshare
This is a simple utility for transferring files, including non-empty directories, between computers over a local network. While it may work over the internet, I haven't tested that functionality. I did test it within a single subnet with a 5TB data load, and the transfer was completely successful.
